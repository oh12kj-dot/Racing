{
  "name": "racing-runtime-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:browser": "playwright test --project=chromium",
    "test:ios": "playwright test --project=iphone-webkit",
    "test:browser:all": "playwright test",
    "test:browser:headed": "playwright test --project=chromium --headed"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
