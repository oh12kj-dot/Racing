export function createProfiler(W,{mobile=false,targetFps=60}={}){
  const r=W.renderer,gl=r.getContext(),ext=gl.getExtension?.('EXT_disjoint_timer_query_webgl2');
  const ema=new Map(),alpha=.12,history=[];let frameStart=0,lastFrameStart=0,lastPaint=0,lastSample=0,open=false,gpuMs=null,pending=null,shadowUpdated=false,longFrames=0,totalFrames=0;
  const avg=(k,v)=>{if(!Number.isFinite(v))return;const old=ema.get(k);ema.set(k,old==null?v:old+(v-old)*alpha);};
  const btn=document.createElement('button');btn.textContent='PERF';btn.style.cssText='position:fixed;z-index:96;right:10px;bottom:max(10px,env(safe-area-inset-bottom));height:30px;min-width:48px;padding:0 7px;border-radius:8px;background:#07121bea;color:#fff;border:1px solid #ffffff35;font:900 9px -apple-system,sans-serif';document.body.appendChild(btn);
  const panel=document.createElement('div');panel.style.cssText='display:none;position:fixed;z-index:95;right:10px;bottom:max(46px,calc(env(safe-area-inset-bottom) + 42px));width:min(90vw,340px);max-height:62vh;overflow:auto;padding:9px;border-radius:10px;background:#07121bf2;border:1px solid #ffffff30;color:#fff;font:700 10px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace;backdrop-filter:blur(10px)';document.body.appendChild(panel);
  btn.onclick=()=>{open=!open;panel.style.display=open?'block':'none';};
  function measure(name,fn){const t=performance.now(),v=fn(),ms=performance.now()-t;avg(name,ms);return v;}
  function gpuBegin(){if(!ext||pending)return null;try{const q=gl.createQuery();gl.beginQuery(ext.TIME_ELAPSED_EXT,q);return q;}catch{return null;}}
  function gpuEnd(q){if(!q||!ext)return;try{gl.endQuery(ext.TIME_ELAPSED_EXT);pending=q;}catch{try{gl.deleteQuery(q);}catch{}}}
  function pollGpu(){if(!ext||!pending)return;try{const ready=gl.getQueryParameter(pending,gl.QUERY_RESULT_AVAILABLE),disjoint=gl.getParameter(ext.GPU_DISJOINT_EXT);if(ready){if(!disjoint){gpuMs=gl.getQueryParameter(pending,gl.QUERY_RESULT)/1e6;avg('GPU',gpuMs);}gl.deleteQuery(pending);pending=null;}}catch{pending=null;}}
  function snapshot(now=performance.now()){
    const info=r.info,interval=ema.get('INTERVAL')||0,work=ema.get('WORK')||0,gpu=ema.get('GPU');
    return{t:Math.round(now),fps:interval>0?1000/interval:0,intervalMs:interval,workMs:work,gpuMs:gpu??null,dpr:r.getPixelRatio(),calls:info.render.calls,triangles:info.render.triangles,geometries:info.memory.geometries,textures:info.memory.textures,longFrameRate:totalFrames?longFrames/totalFrames:0,targetFps};
  }
  function sample(now){if(now-lastSample<2000)return;lastSample=now;history.push(snapshot(now));while(history.length>90)history.shift();}
  async function copyLog(){const payload={version:'v38',ua:navigator.userAgent,mobile,targetFps,samples:history.slice(-60),latest:snapshot()};const text=JSON.stringify(payload,null,2);try{await navigator.clipboard?.writeText(text);return true;}catch{try{const ta=document.createElement('textarea');ta.value=text;document.body.appendChild(ta);ta.select();document.execCommand('copy');ta.remove();return true;}catch{return false;}}}
  function paint(now){if(!open||now-lastPaint<500)return;lastPaint=now;const info=r.info,interval=ema.get('INTERVAL')||0,work=ema.get('WORK')||0,fps=interval?1000/interval:0,gpu=ema.get('GPU'),budget=1000/Math.max(1,targetFps),entries=[...ema.entries()].filter(([k])=>!['INTERVAL','WORK','GPU'].includes(k)).sort((a,b)=>b[1]-a[1]).slice(0,8);panel.innerHTML=`<b>RUNTIME PROFILER v38 ${mobile?'· MOBILE':''}</b><br>ACTUAL FPS ${fps.toFixed(1)} · INTERVAL ${interval.toFixed(2)} ms<br>CPU WORK ${work.toFixed(2)} ms · GPU ${gpu==null?'N/A':gpu.toFixed(2)+' ms'}<br>BUDGET ${budget.toFixed(2)} ms · DPR ${r.getPixelRatio().toFixed(2)}<br>CALLS ${info.render.calls} · TRI ${(info.render.triangles/1000).toFixed(0)}k · LONG ${(totalFrames?longFrames/totalFrames*100:0).toFixed(1)}%<br>GEO ${info.memory.geometries} · TEX ${info.memory.textures}<br>SHADOW ${shadowUpdated?'UPDATED':'CACHED'}<hr style="border:0;border-top:1px solid #ffffff22">${entries.map(([k,v],i)=>`${i+1}. ${k.padEnd(13,' ')} ${v.toFixed(2)} ms`).join('<br>')}<br><button id="v38CopyPerf" style="margin-top:8px;height:30px;background:#fff;color:#07121b;border:0;border-radius:6px;font-weight:900">COPY PERF LOG</button><span id="v38CopyState" style="margin-left:7px;opacity:.7"></span>`;panel.querySelector('#v38CopyPerf')?.addEventListener('click',async()=>{const ok=await copyLog();const s=panel.querySelector('#v38CopyState');if(s)s.textContent=ok?'COPIED':'COPY FAILED';});}
  function beginFrame(now=performance.now()){frameStart=now;if(lastFrameStart){const interval=now-lastFrameStart;avg('INTERVAL',interval);totalFrames++;if(interval>(1000/Math.max(1,targetFps))*1.35)longFrames++;}lastFrameStart=now;shadowUpdated=false;pollGpu();}
  function endFrame({shadow=false,workMs=null}={}){shadowUpdated=shadow;const now=performance.now(),work=Number.isFinite(workMs)?workMs:now-frameStart;avg('WORK',work);sample(now);paint(now);}
  const api={measure,beginFrame,endFrame,gpuBegin,gpuEnd,record:(name,ms)=>avg(name,ms),snapshot,copyLog,get actualFps(){const v=ema.get('INTERVAL');return v?1000/v:0;},get frameIntervalMs(){return ema.get('INTERVAL')||0;},get workMs(){return ema.get('WORK')||0;},get gpuMs(){return ema.get('GPU')??null;},get history(){return history.slice();}};
  try{window.__RACING_PERF__=api;}catch{}
  return api;
}
