import {createRace as createV8Race} from './v8-race-final.js';

export function createRace(W,statusEl,settings={}){
  const base=createV8Race(W,statusEl),T=W.THREE,total=W.total,clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
  base.race.lapsTarget=Math.max(2,Math.min(30,Number(settings.laps)||6));
  const classes=['formula','gt','proto','hyper','touring','supercar','lmh'];
  const classPerf={formula:[1.035,1.04,1.03],gt:[.985,.99,1],proto:[1.01,1.015,1.01],hyper:[1.02,1.01,.995],touring:[.955,.97,1.02],supercar:[.995,.985,.98],lmh:[1.018,1.025,1.015]};
  for(const c of base.cars){
    const type=classes[c.id%classes.length],old=c.mesh,mesh=W.makeCar(c.teamColor||0xffffff,type),q=W.sample(c.s,c.lane);
    old?.parent?.remove(old);W.scene.add(mesh);mesh.position.copy(q.p);mesh.position.y+=.12;mesh.rotation.y=Math.atan2(q.t.x,q.t.z);
    c.mesh=mesh;c.type=type;c.length=mesh.userData.dims.length;c.width=mesh.userData.dims.width;
    const [speed,accel,brake]=classPerf[type];c.baseMaxNominal*=speed;c.accelNominal*=accel;c.brakeNominal*=brake;
    c._v10CoreMax=c.baseMaxNominal;c._v10CoreAccel=c.accelNominal;c._v10CoreBrake=c.brakeNominal;
    c.tyreTemp=c.compound==='WET'?72:84;c.brakeTemp=330;c.tempGrip=1;c.trackWarnings=0;c.totalTrackWarnings=0;c.penaltySeconds=0;c.penaltiesServed=0;c.driveThrough=false;c._offTrack=0;c._pitSpeedWarned=false;c._penaltyStopSeen=false;c._lastPitState=c.pitState;c._lastWear=c.wear;
    if(settings.safetyCar===false)c.driver.errorRate=0;
    const f=Math.max(0,Number(settings.failures??1));c.machine.reliability=1-(1-c.machine.reliability)*f;
  }
  const radio=(car,text,kind='INFO')=>{const r=base.radio;if(!r)return;r.push({id:`v10-${Date.now()}-${Math.random()}`,t:base.race.t,carId:car?.id??null,name:car?.name||'RACE CONTROL',text,kind});while(r.length>20)r.shift();};
  const event=(type,car,data={})=>{base.events.push({id:`v10-${Date.now()}-${Math.random()}`,type,t:base.race.t,carId:car?.id??null,data});while(base.events.length>80)base.events.shift();};
  let formation=false,formationDone=false,formationClock=0,gridSnapshot=null;
  function startFormation(){if(formationDone||formation)return;formation=true;formationClock=0;gridSnapshot=base.cars.map(c=>({s:c.s,lane:c.lane,lap:c.lap,progress:c._v8Progress??0,position:c.position}));base.cars.forEach(c=>{c._formationStart=c.s;c._formationTravel=0;c.v=22.2;});radio(null,'FORMATION LAP · TYRES AND BRAKES','CONTROL');}
  function pose(c,lane=c.lane){const q=W.sample(c.s,lane);c.mesh.position.copy(q.p);c.mesh.position.y+=.12;c.mesh.rotation.y=Math.atan2(q.t.x,q.t.z);}
  function updateFormation(dt){formationClock+=dt;statusEl.textContent='FORMATION LAP';const travelRate=total/18;for(const c of base.cars){c._formationTravel+=travelRate*dt;c.s=((c._formationStart+c._formationTravel)%total+total)%total;c.v=22.2;c.lane=T.MathUtils.lerp(c.lane,0,1-Math.exp(-.7*dt));pose(c);}if(formationClock>=18){base.cars.forEach((c,i)=>{const g=gridSnapshot[i];c.s=g.s;c.lane=g.lane;c.laneTarget=g.lane;c.lap=g.lap;c._v8Progress=g.progress;c.position=g.position;c.v=0;c.lastLapStart=0;c.sectorStart=0;pose(c);});formation=false;formationDone=true;radio(null,'FORMATION COMPLETE · GRID READY','CONTROL');}}
  function applyPenalties(c,dt){if(c.retired)return;if(c.pitState==='NONE'&&Math.abs(c.lane)>3.72&&c.v>12)c._offTrack+=dt;else c._offTrack=Math.max(0,c._offTrack-dt*2);if(c._offTrack>.65){c._offTrack=0;c.trackWarnings++;c.totalTrackWarnings++;radio(c,`TRACK LIMITS WARNING ${c.trackWarnings}/3`,'PENALTY');if(c.trackWarnings>=3){c.trackWarnings=0;c.penaltySeconds+=5;event('PENALTY',c,{seconds:5,reason:'TRACK LIMITS'});radio(c,'5 SECOND PENALTY · TRACK LIMITS','PENALTY');}}const pitZone=c.s>total*.90||c.s<total*.11;if(c.pitState!=='NONE'&&pitZone&&c.v>27&&!c._pitSpeedWarned){c._pitSpeedWarned=true;c.penaltySeconds+=5;radio(c,'5 SECOND PENALTY · PIT SPEED','PENALTY');event('PENALTY',c,{seconds:5,reason:'PIT SPEED'});}if(c.pitState==='NONE')c._pitSpeedWarned=false;if(c.totalTrackWarnings>=6&&!c.driveThrough){c.driveThrough=true;radio(c,'DRIVE THROUGH PENALTY','PENALTY');}if(c.driveThrough&&c.pitState==='NONE'&&c.s>total*.79){c.pitState='ENTRY';c.driveThrough=false;c._driveThroughServing=true;}if(c._lastPitState!=='STOP'&&c.pitState==='STOP'){if(c._driveThroughServing){c.pitTimer=Math.min(c.pitTimer,.18);c._driveThroughServing=false;c.penaltiesServed++;radio(c,'DRIVE THROUGH SERVED','PENALTY');}else if(c.penaltySeconds>0){c.pitTimer+=c.penaltySeconds;c.penaltiesServed+=c.penaltySeconds;radio(c,`${c.penaltySeconds}s PENALTY SERVED`,'PENALTY');c.penaltySeconds=0;}}c._lastPitState=c.pitState;}
  function pitVisual(c){if(c.pitState==='NONE')return;const lane=W.pitLaneOffset||13.2;let q;if(c.pitState==='STOP'){const boxS=total*(.006+c.teamId*.0045);q=W.sample(boxS,lane);}else q=W.sample(c.s,lane);c.mesh.position.copy(q.p);c.mesh.position.y+=.12;c.mesh.rotation.y=Math.atan2(q.t.x,q.t.z);}
  function preTemperature(c){const optimal=c.compound==='WET'?72:92,d=Math.abs(c.tyreTemp-optimal),grip=clamp(1.015-d/185,.84,1.015),ai=clamp(Number(settings.ai)||1,.82,1.18);c.tempGrip=grip;c.baseMaxNominal=c._v10CoreMax*grip*ai;c.accelNominal=c._v10CoreAccel*(.94+.06*grip)*ai;c.brakeNominal=c._v10CoreBrake*(c.brakeTemp>850?.88:c.brakeTemp<220?.94:1);}
  function postTemperature(c,dt,wearBefore){const wet=W.env?.wetness||0,speed=c.v,braking=c.brakeVisual||0;const tyreTarget=(c.compound==='WET'?68:80)+speed*.12+braking*8-wet*18,brakeTarget=240+speed*2.0+braking*510;c.tyreTemp=T.MathUtils.lerp(c.tyreTemp,tyreTarget,1-Math.exp(-.12*dt));c.brakeTemp=T.MathUtils.lerp(c.brakeTemp,brakeTarget,1-Math.exp(-.45*dt));const mult=clamp(Number(settings.tyreWear)||1,.25,3),delta=Math.max(0,c.wear-wearBefore);c.wear=clamp(wearBefore+delta*mult,0,1);}
  const historyKey='racing_v10_history';function loadHistory(){try{const h=JSON.parse(localStorage.getItem(historyKey)||'[]');return Array.isArray(h)?h:[];}catch{return[];}}const history=loadHistory();let resultSaved=false;function saveHistory(){try{localStorage.setItem(historyKey,JSON.stringify(history.slice(-40)));}catch{}}
  function maybeSaveResult(){if(resultSaved||!base.championship?.awarded)return;const st=base.getStandings(),pole=base.cars[base.qualifying?.[0]?.carId],fast=[...base.cars].filter(c=>Number.isFinite(c.bestLap)).sort((a,b)=>a.bestLap-b.bestLap)[0];history.push({date:new Date().toISOString(),circuit:W.circuitName||'SUZUKA',round:base.championship.roundName,winner:st[0]?.name||'--',podium:st.slice(0,3).map(c=>c.name),pole:pole?.name||'--',fastest:fast?.name||'--',dnf:base.cars.filter(c=>c.retired).map(c=>c.name)});saveHistory();resultSaved=true;}
  function stats(){const names=base.cars.map(c=>c.name),out=names.map(name=>({name,wins:0,podiums:0,poles:0,fastest:0,dnfs:0})),by=Object.fromEntries(out.map(x=>[x.name,x]));for(const h of history){if(by[h.winner])by[h.winner].wins++;for(const n of h.podium||[])if(by[n])by[n].podiums++;if(by[h.pole])by[h.pole].poles++;if(by[h.fastest])by[h.fastest].fastest++;for(const n of h.dnf||[])if(by[n])by[n].dnfs++;}return out.sort((a,b)=>b.wins-a.wins||b.podiums-a.podiums);}
  const originalUpdate=base.update;
  function update(dt){if(!formationDone&&!formation){const before=base.sessionPhase;originalUpdate(dt);if(before==='QUALIFYING'&&base.sessionPhase!=='QUALIFYING')startFormation();return;}if(formation){updateFormation(dt);return;}const wearBefore=base.cars.map(c=>c.wear);for(const c of base.cars)preTemperature(c);originalUpdate(dt);for(let i=0;i<base.cars.length;i++){const c=base.cars[i];postTemperature(c,dt,wearBefore[i]);applyPenalties(c,dt);pitVisual(c);}maybeSaveResult();}
  const circuitRoundNames=['SUZUKA GP','FUJI STYLE GP','MONZA STYLE GP','SPA STYLE GP','SUZUKA FINAL'];
  return new Proxy(base,{get(target,prop){if(prop==='update')return update;if(prop==='sessionPhase')return formation?'FORMATION':Reflect.get(target,prop,target);if(prop==='formation')return formation;if(prop==='historyStats')return{history:[...history].reverse(),drivers:stats()};if(prop==='championship'){const c=Reflect.get(target,prop,target);return c?{...c,roundName:circuitRoundNames[c.round]||c.roundName}:c;}return Reflect.get(target,prop,target);}});
}
