[package]
name = "sim-math"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Deterministic math foundation for the race simulation core."

# 本体依存はゼロ。決定性を外部クレートの実装変更に依存させないため
# （ADR-0001 / DECISIONS.md）。依存追加には Opus の承認が必要。
[dependencies]

[dev-dependencies]
approx = "0.5"
